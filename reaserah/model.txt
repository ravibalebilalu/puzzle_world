from django.db import models
# in front end we hahve to create 12 * 12 grid to display all characters (eal and random),
# each box in grid  will have a unique index : 12x12 = 144 -> 0 - to 143 
class Char(models.Model):
    # building block of puzzle
    # it should contain iformation about single character of a word : 
    # 1. character character of word  -> this will be assigned during grid creation
    # 2.   index in grid ->(0 -> 143) this will be assigned during grid creation
    # 3. char_checked  default: False ->  this will be changed while playing puzzel.if player marked a word correctly 
    #     the corresponding char will marked as True

    pass

class Word(models.model):
    # word : this will be assigned during model creation ,we have to use a seperate code to process 
    # text file content ,process,and finally get meaningfull words ,
    # word_checked :default: False  -> this will be asigned during play ,if user finds correct word then 
    # a function should change it to true
    pass

class Puzzle(models.model):
    # row_ength : default : 12 we can change it to any number
    # col_ength : default : 12 we can change it to any number
            # both row and col should have max limit as 15 and min limit as 10 
    # grid_checked : default :False  ,after compleating all words in gris it shouls turned to true
    
    pass
     
-------------------------------done-----------------------------------------